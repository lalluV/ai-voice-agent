# Proxy admin API calls to the deployed voice agent (no local uvicorn needed).
# For a fully local stack, set: VITE_PROXY_TARGET=http://127.0.0.1:8080
VITE_PROXY_TARGET=https://voice-agent.lalluvemula.cloud
